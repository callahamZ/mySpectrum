import 'package:flutter/material.dart';

class DataRecordPage extends StatelessWidget {
  const DataRecordPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Data Record Page'));
  }
}